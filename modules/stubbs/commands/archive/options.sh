# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Thu Nov 29 12:18:44 PST 2012
#
#/ usage: stubbs:archive [ --file|-f <>] [ --format|-f <bin>]  --modules <'*'> [ --release <1>] [ --version <1.0.0>] [ --template <>]

# _rerun_options_parse_ - Parse the command arguments and set option variables.
#
#     rerun_options_parse "$@"
#
# Arguments:
#
# * the command options and their arguments
#
# Notes:
# 
# * Sets shell variables for any parsed options.
# * The "-?" help argument prints command usage and will exit 2.
# * Return 0 for successful option parse.
#
rerun_options_parse() {
    
    while [ "$#" -gt 0 ]; do
        OPT="$1"
        case "$OPT" in
            --file|-f) rerun_option_check $# ; FILE=$2 ; shift ;;
            --format|-f) rerun_option_check $# ; FORMAT=$2 ; shift ;;
            --modules) rerun_option_check $# ; MODULES=$2 ; shift ;;
            --release) rerun_option_check $# ; RELEASE=$2 ; shift ;;
            --version) rerun_option_check $# ; VERSION=$2 ; shift ;;
            --template) rerun_option_check $# ; TEMPLATE=$2 ; shift ;;
            # help option
            -?)
                rerun_option_usage
                exit 2
                ;;
            # end of options, just arguments left
            *)
              break
        esac
        shift
    done

    # Set defaultable options.
    [ -z "$FORMAT" ] && FORMAT="bin"
    [ -z "$MODULES" ] && MODULES="'*'"
    [ -z "$RELEASE" ] && RELEASE="1"
    # Check required options are set
    [ -z "$MODULES" ] && { echo >&2 "missing required option: --modules" ; return 2 ; }
    # If option variables are declared exportable, export them.

    #
    return 0
}


# Initialize the options variables to null.
FILE=
FORMAT=
MODULES=
RELEASE=
VERSION=
TEMPLATE=

